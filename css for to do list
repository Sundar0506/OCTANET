body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    background: linear-gradient(to bottom, #ffafbd, #ffc3a0); 
}
.container {
    display: flex;
    flex-direction: column;
    flex: 1;
}
header {
    background-color: #ff6f61; 
    color: white;
    text-align: center;
    padding: 20px;
}

.title {
    margin: 0;
}
main {
    flex: 1;
    padding: 20px;
    background-color: #ffffff; 
    border-radius: 8px; 
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); 
}

.scroll-container {
    max-height: 60vh;
    overflow-y: auto;
}
#task-list {
    list-style-type: none;
    padding: 0;
    margin: 0;
}

#task-list li {
    display: flex;
    align-items: center;
    padding: 10px;
    border-bottom: 1px solid #ddd;
}

#task-list input[type="checkbox"] {
    margin-left: auto;
    margin-right: 10px;
}
footer {
    background-color: #ff6f61;
    padding: 10px;
    text-align: center;
    color: white;
}

footer nav ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
}

footer nav ul li {
    display: inline;
    margin: 0 10px;
}

footer nav ul li a {
    text-decoration: none;
    color: #000000; 
}

footer nav ul li a:hover {
    text-decoration: underline;
}
#todo-form {
    display: flex;
    justify-content: center;
    margin-top: 20px;
}

#new-task {
    margin-right: 10px;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
}

button {
    padding: 10px 20px;
    border: none;
    border-radius: 4px;
    background-color: #ff6f61; 
    color: white;
    cursor: pointer;
}

button:hover {
    background-color: #e85d4f;/* Darker coral pink on hover */
}
